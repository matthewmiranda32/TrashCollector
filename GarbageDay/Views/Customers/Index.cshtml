@model IEnumerable<GarbageDay.Models.Customer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">

    <tr>
        <p>The people represented in this database are purely fictitious. Whatever resemblance they bear to any real persons, living or dead, is a miracle.</p>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AddressBook.streetAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AddressBook.city)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AddressBook.state)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AddressBook.customerZipCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pickup_Day)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Payment)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
if (item.Name != null)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AddressBook.streetAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AddressBook.city)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AddressBook.state)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AddressBook.customerZipCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pickup_Day)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Payment)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
            @Html.ActionLink("Details", "Details", new { id = item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.id })
        </td>
    </tr>
    }
}

</table>
